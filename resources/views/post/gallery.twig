{% extends "layouts.app" %}

{% block title %}{{ trans('Galerie') }}{% endblock %}

{% block content %}

    <h2 class="text-primary text-3xl mb-6 mt-12 text-center">Galerie Freundschaftslauf</h2>

    {% if posts|length > 0 %}
        <div class="-mx-2 sm:flex sm:flex-wrap">
            {% for post in posts %}
                <div class="block sm:w-1/2 p-3 sm:flex-none">
                    {% include "post/preview" with {post: post, edit: false, report: true} %}
                </div>
            {% endfor %}
        </div>

        {{ posts.links() }}
    {% else %}
            <p class="text-gray-150 mb-3">Liebe Läufer*innen, auf euren Teamseiten könnt ihr ab dem 20.05. eure Bilder und Texte hochladen. Bis dahin könnt ihr eure Erlebisse mit uns auf Facebook teilen. Wir freuen uns auf die Eindrücke eures ganz individuellen Laufes.</p>
     {% endif %}

    </div>
{% endblock %}

{% block head %}
    <link rel="stylesheet" type="text/css" href="/slick/slick/slick.css"/>
    <link rel="stylesheet" type="text/css" href="/slick/slick/slick-theme.css"/>
{% endblock %}

{% block scripts %}
<script src="{{ mix('js/jquery.min.js') }}" type="text/javascript"></script>
<script src="{{ mix('js/slick.min.js') }}" type="text/javascript"></script>
<script src="{{ mix('js/init.slick.js') }}" type="text/javascript"></script>
{% endblock %}
