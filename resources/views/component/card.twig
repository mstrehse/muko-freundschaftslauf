{% if link_to %}<a href="{{ link_to }}">{% endif %}
    <div class="card py-30 px-20">
        {% block content %}
            <p class="text-sm">Kein Contentblock angegeben.</p>
        {% endblock %}
    </div>
{% if link_to %}</a>{% endif %}