<h2 class="text-2xl mb-6">Ergebnis löschen</h2>


<form method="post" action="{{ route('result.delete', {result: result}) }}">
    {{ csrf_field() }}

    <p class="mb-6">Möchten Sie dieses Ergebnis wirklich entgültig löschen?</p>

    <div class="flex flex-wrap mb-5 relative">
        <button>
            {% include "component/button/button" with {text: "Ergebnis löschen", cta: true} %}
        </button>
    </div>
</form>
